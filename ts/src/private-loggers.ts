import { Logger } from './logger';

export const DOM_TOOLS_LOGGER = new Logger().withName("dom-tools.ts");

export const PLUGIN_LOGGER = new Logger().withName("plugin.ts");

export const CORE_LOGGER = new Logger().withName("SOURC_CORE");
